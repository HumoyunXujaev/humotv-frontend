# 🎬 HumoTV - Стриминговый Сервис

> Привет! Это мой пет-проект стримингового сервиса, который я сделал как тестовое задание для позиции стажера. Честно говоря, это был челлендж - сделать что-то похожее на Netflix, но с нуля. И знаешь что? Получилось неплохо!

## 🤔 Что это вообще?

Это полноценный стриминговый сервис с бэкендом на FastAPI и мобильным приложением на Android. Можно регистрироваться, создавать профили (как "Кто смотрит?" в Netflix), смотреть фильмы и сериалы, добавлять в "Мой список", и все это работает!

## 🛠 Технологии

### Бэкенд
- **FastAPI** - выбрал его потому что быстрый, современный, и автоматически генерирует документацию API (Swagger)
- **PostgreSQL** - надежная БД, которую я уже знал
- **SQLAlchemy 2.0** - ORM с поддержкой async/await, было немного сложно разобраться с новым синтаксисом
- **Alembic** - для миграций БД, очень удобно когда меняешь модели
- **MinIO** - это как S3, но свой. Туда складываются все видео, постеры, аватарки
- **Docker Compose** - чтобы все запускалось одной командой
- **JWT** - для аутентификации с access и refresh токенами

### Мобилка
- **Android** - Kotlin + Jetpack Compose
- **Jetpack Compose** - современный UI-фреймворк от Google, декларативный подход как в React
- **Material Design 3** - следовал гайдлайнам Google, получился темный Netflix-style дизайн
- **ExoPlayer** - видеоплеер от Google, поддерживает кучу форматов
- **Retrofit** - для работы с API
- **Coil** - для загрузки изображений
- **DataStore** - для хранения токенов, лучше чем SharedPreferences
- **Navigation Component** - для навигации между экранами

## 🎯 Что умеет проект?

### Аутентификация и профили
- **Регистрация/Вход** - классика, ничего сложного
- **JWT токены** - сделал access (живет 15 минут) и refresh (неделя). Когда access истекает, автоматически обновляется через refresh
- **Система профилей** - как в Netflix! Один аккаунт = до 5 профилей. У каждого своя история просмотра и избранное
- **Аватарки** - можно загрузить свое фото для профиля

### Каталог контента
- **Фильмы и Сериалы** - разделил через полиморфизм (одна базовая модель Media, от нее наследуются Movie и Series)
- **Жанры** - можно фильтровать контент по жанрам (Боевик, Комедия, Драма и т.д.)
- **Детальная страница** - описание, рейтинг, актеры, год выпуска
- **Для сериалов** - список эпизодов по сезонам, можно выбрать что смотреть

### Стриминг видео
Это было самое сложное, если честно:
- **HTTP Range Requests** - позволяет перематывать видео на любое место без загрузки всего файла
- **Прогресс просмотра** - сохраняется каждые 10 секунд, чтобы можно было продолжить с того же места
- **ExoPlayer** - настроил кеширование и обработку ошибок

### Фичи как в Netflix

#### "Продолжить просмотр"
- Автоматически показывает фильмы/серии, которые ты начал смотреть
- Есть прогресс-бар показывающий сколько просмотрено
- Можно кликнуть и продолжить с того же места

#### "Мой список"
- Добавляй/удаляй контент в избранное
- Кнопка с сердечком меняет цвет (красное = в избранном)
- У каждого профиля свой список

#### Админские подборки
- Админ может создавать подборки типа "Новинки", "Топ-10", "Рекомендуем"
- Отображаются на главной странице горизонтальными рядами

### Поиск
- Полнотекстовый поиск по названию и описанию
- Работает для фильмов и сериалов одновременно
- Минимум 3 символа для поиска (чтобы не грузить сервер)

### UX штуки
- **Pull-to-Refresh** - потяни вниз на главной, чтобы обновить
- **Skeleton loaders** - вместо простого спиннера показываются "призраки" карточек
- **Обработка ошибок** - когда нет интернета или API упал, показывается нормальное сообщение
- **Bottom Navigation** - классный таббар внизу для быстрой навигации

### Админка
- Есть админ-панель на SQLAdmin
- Можно добавлять фильмы, сериалы, эпизоды
- Загружать видео и постеры
- Создавать подборки
- Управлять пользователями

## 🏗 Архитектура

### Бэкенд структура
```
src/
├── models/          # SQLAlchemy модели (User, Profile, Movie, Series...)
├── schemas/         # Pydantic схемы для валидации и сериализации
├── services/        # Бизнес-логика (AuthService, MediaService...)
├── api/
│   └── endpoints/   # Роутеры FastAPI
├── core/            # Конфиги, БД setup
└── admin/           # Админ-панель
```

### Мобилка структура
```
app/src/main/java/com/humotv/humotv/
├── screens/         # Compose экраны (Home, Detail, Player...)
├── MainActivity     # Entry point
├── Navigation       # Навигация между экранами
├── ApiService       # Retrofit интерфейсы
├── Models           # Data классы
└── TokenManager     # Управление JWT токенами
```

## 📊 Модели БД

### Полиморфизм для медиа
Использовал Single Table Inheritance - все медиа в одной таблице, но с типом:
```python
class Media(BaseModel):
    __tablename__ = "media"
    media_type = Column(String)  # 'movie' или 'series'
    
class Movie(Media):
    __mapper_args__ = {"polymorphic_identity": "movie"}
    runtime_minutes = Column(Integer)  # только у фильмов
    
class Series(Media):
    __mapper_args__ = {"polymorphic_identity": "series"}
    seasons = relationship("Season")  # только у сериалов
```

### Профили и связи
```python
User -> Profile (1 к многим)
Profile -> WatchHistory (1 к многим)  # история просмотра
Profile -> UserFavorite (1 к многим)  # избранное
```

## 🚀 Как запустить?

### Бэкенд

1. **Клонируй репо:**
```bash
git clone <твой-репо>
cd backend
```

2. **Создай .env файл:**
```env
# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=humotv_db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# MinIO
MINIO_ENDPOINT=http://minio:9000
MINIO_ENDPOINT_HTTP=http://minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=humotv

# JWT
SECRET_KEY=твой-секретный-ключ-генерируй-рандомно
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# Superuser (создается автоматически при старте)
SUPERUSER_EMAIL=admin@example.com
SUPERUSER_PASSWORD=admin123
```

3. **Запусти все сразу:**
```bash
docker-compose up -d
```

Это запустит:
- PostgreSQL на порту 5432
- MinIO на порту 9000 (API) и 9001 (UI)
- FastAPI бэкенд на порту 8000

4. **Проверь что работает:**
- API: http://localhost:8000/health
- Swagger Docs: http://localhost:8000/docs
- Админка: http://localhost:8000/admin (логин: admin@example.com / admin123)
- MinIO Console: http://localhost:9001 (логин: minioadmin / minioadmin)

### Мобилка

1. **Открой в Android Studio:**
```bash
cd mobile
# Открой папку в Android Studio
```

2. **Измени BASE_URL** (если нужно):
```kotlin
// В ApiService.kt
const val BASE_URL = "http://10.0.2.2:8000/api/v1/"  // для эмулятора
// const val BASE_URL = "http://192.168.1.X:8000/api/v1/"  // для реального девайса
```

3. **Синкни Gradle и запусти:**
- Build -> Make Project
- Run -> Run 'app'

## 💾 Как добавить контент?

### Через админку (проще):
1. Открой http://localhost:8000/admin
2. Войди как admin
3. Genres -> Create - создай жанры
4. Movies/Series -> Create - добавь контент
5. Video Files -> Upload - загрузи видео файлы
6. Collections -> Create - сделай подборку

### Через API (для программистов):
```python
# Сначала получи токен
POST /api/v1/auth/login
{
  "email": "admin@example.com",
  "password": "admin123"
}

# Создай жанр
POST /api/v1/media/genres/
Authorization: Bearer <твой-токен>
{
  "name": "Боевик"
}

# Загрузи видео (multipart/form-data)
POST /api/v1/upload/video
Authorization: Bearer <твой-токен>
file: <твой-видео-файл.mp4>

# Создай фильм
POST /api/v1/media/movies/
Authorization: Bearer <твой-токен>
{
  "title": "Крутой фильм",
  "description": "Очень крутой",
  "release_year": 2024,
  "rating": 8.5,
  "poster_url": "posters/poster.jpg",
  "video_file_id": 1,
  "genre_ids": [1]
}
```

## 🔥 Сложности, с которыми столкнулся

### 1. HTTP Range Requests
Это было неочевидно! Нужно было:
- Читать `Range` header из запроса
- Парсить байтовые диапазоны (bytes=0-1023)
- Отдавать частично контент с статусом 206
- Правильно устанавливать `Content-Range` header

Зато теперь можно перематывать видео и оно не грузится заново!

### 2. Полиморфизм в SQLAlchemy 2.0
Долго разбирался как сделать Movie и Series от одной базовой модели. Проблемы были с:
- `__mapper_args__` и `polymorphic_identity`
- Загрузкой связанных данных (seasons, episodes)
- Работой с async sessions

Но в итоге работает красиво - один эндпоинт возвращает и фильмы и сериалы.

### 3. ExoPlayer и состояние
ExoPlayer нужно правильно освобождать при выходе из экрана, иначе:
- Утечки памяти
- Видео продолжает играть в фоне
- Краш при повороте экрана

Решил через `DisposableEffect` в Compose.

### 4. Алембик миграции
Когда менял модели, миграции иногда генерировались неправильно. Приходилось:
- Редактировать вручную
- Иногда дропать БД и накатывать с нуля
- Внимательно проверять что генерируется

### 5. MinIO пути на мобилке
Бэкенд отдает просто `"posters/movie1.jpg"`, а нужен полный URL. Сделал функцию которая:
- Заменяет `8000` на `9000` в BASE_URL
- Добавляет bucket name
- Собирает полный путь

Но правильнее было бы бэкенду отдавать сразу полные URL.

## 📱 Фичи мобилки в деталях

### Auth Flow
1. **Splash Screen** - проверяет есть ли токен
2. **Welcome** - если нет токена, показывает кнопки "Войти" / "Регистрация"
3. **Login/Register** - формы входа
4. **Profile Select** - экран "Кто смотрит?"
5. **Main App** - основное приложение с таббаром

### Главная страница (Home)
Показывает:
- **Hero Banner** - большой баннер с главным контентом
- **"Продолжить просмотр"** - горизонтальный ряд с прогресс-барами
- **"Мой список"** - твое избранное
- **Админские подборки** - все подборки по очереди

Есть Pull-to-Refresh для обновления.

### Детальная страница
- Постер на весь экран с градиентом
- Название, описание, рейтинг, год, жанры
- Кнопки "Играть" и "Добавить в список"
- Для сериалов - список эпизодов по сезонам
- Можно развернуть описание если длинное

### Плеер
- Полноэкранный режим
- Кастомные контроллы
- Показывает название
- Сохраняет прогресс каждые 10 секунд
- Можно перематывать (благодаря Range Requests)

### Поиск
- Real-time поиск с debounce
- Grid из карточек
- Показывает тип (фильм/сериал)
- Переход на детальную страницу

### Мой список
- Grid избранного контента
- Можно удалить свайпом (не реализовал, но можно добавить)
- Пустое состояние если ничего не добавлено

### Профиль (Account)
- Информация о юзере
- Аватарка (можно загрузить с камеры/галереи)
- Смена пароля
- Кнопка выхода

## 🔐 Безопасность

### Что сделал:
- ✅ Пароли хешируются bcrypt
- ✅ JWT токены с коротким временем жизни
- ✅ Refresh token для обновления
- ✅ Защита всех эндпоинтов через depends
- ✅ Админ-панель требует superuser права

### Что надо улучшить:
- ⚠️ CORS открыт для всех (для демо)
- ⚠️ MinIO bucket публичный
- ⚠️ Нет rate limiting
- ⚠️ Нет HTTPS

## 📈 Что можно добавить?

### Если б было время:
1. **Рейтинги и отзывы** - пользователи могут ставить оценки
2. **Рекомендации** - ML алгоритм на основе истории
3. **Уведомления** - Push когда выходит новая серия
4. **Субтитры** - поддержка SRT файлов
5. **Трейлеры** - превью перед просмотром
6. **Родительский контроль** - ограничение по возрасту
7. **Мультиязычность** - i18n для интерфейса
8. **Загрузка оффлайн** - скачать и смотреть без интернета
9. **Chromecast** - трансляция на ТВ
10. **Social features** - делиться с друзьями

### Для продакшена нужно:
1. **Транскодинг видео** - разные разрешения (720p, 1080p, 4K)
2. **CDN** - для быстрой доставки контента по всему миру
3. **Redis** - для кеширования
4. **Elasticsearch** - для лучшего поиска
5. **Мониторинг** - Prometheus + Grafana
6. **Логирование** - ELK stack
7. **Тесты** - Unit, Integration, E2E
8. **CI/CD** - автоматическая сборка и деплой

## 📚 Что я узнал делая это

1. **FastAPI** - как делать быстрые API с автодокументацией
2. **Async Python** - async/await, asyncio, async SQLAlchemy
3. **Полиморфизм в БД** - STI, MTI, конкретная таблица
4. **MinIO/S3** - работа с object storage
5. **JWT** - как правильно делать аутентификацию
6. **HTTP Range** - как работает стриминг видео
7. **Jetpack Compose** - современный UI для Android
8. **MVVM на Android** - архитектурный паттерн
9. **Docker Compose** - оркестрация нескольких контейнеров
10. **ExoPlayer** - сложный но мощный видеоплеер

## 🤝 Благодарности

Спасибо за тестовое задание! Было интересно и сложно одновременно. Научился многому новому, особенно с полиморфизмом и стримингом.

## 📞 Контакты

Если будут вопросы по проекту или хотите обсудить детали реализации - пишите!

---

**P.S.** Да, я знаю что это не идеальный код и есть что улучшать. Но для стажерской позиции и ограниченного времени - я горжусь результатом! А И ЕЩЕ ИЗВИНЯЮСЬ,ЧТО НЕ НАПИСАЛ КОММЕНТАРИИ, Я ОБЫЧНО ИХ ДОБАВЛЯЮ ПРИ РЕФАКТОРИНГЕ ТОЛЬКО, В ЭТОТ РАЗ НЕ УСПЕЛ 🚀

**P.P.S.** Если запускаете - добавьте контента через админку, а то будет пусто 😅
